name: generic_company_application
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.10.3


dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.0.0

  cupertino_icons: ^1.0.8

  #FireBase Files
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  firebase_database: ^12.1.0
  
  #Accesing Image from gallery
  image_picker: ^1.2.1

  #Navigation
  go_router: ^17.0.1
   
  #Local Storage for username,email.
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  